export enum Status {
    PendingPayment = 'PendingPayment',
    Pending = 'Pending',
    Active = 'Active',
    Inactive = 'Inactive',
}

export enum Gender {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other',
}

export enum DietStatus {
    Draft = 'Draft',
    Active = 'Active',
    Finished = 'Finished',
}